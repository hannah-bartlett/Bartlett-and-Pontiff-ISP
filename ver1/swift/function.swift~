/*import Foundation

var stock1 = 50
var stock2 = 50

func refreshStocks(stock: Int) {
    var stockValue = 0
    if stock == 1 {
        stockValue = stock1
    }
    if stock == 2 {
        stockValue = stock2
    }
    let changeAmount = Int.random(in:1..<50)
    let changeType = Int.random(in:1..<3)
    if changeType == 1 {
        print("Adding \(changeAmount)"
        stockValue += changeAmount
        print("New price: \(stockValue)"

        var randomInt = 0
        var randomInt1 = 0
        randomInt = Int.random(in:1..<100)
        randomInt1 = Int.random(in:1..<100)
        print("What is \(randomInt) + \(randomInt)?")
        let answer = readLine()
        if answer == randomInt + randomInt {
            print("Correct!")
        }

        
 */
